server:
  type: default-bounded
  maxQueuedRequests: 2048
  # Never change the following param!
  rootPath: /api

mongoUri: "${ZUCCHINI_MONGO_URL:-mongodb://mongo/zucchini?w=majority&journal=true}"

logging:
  level: "${ZUCCHINI_LOGGING_LEVEL:-INFO}"
